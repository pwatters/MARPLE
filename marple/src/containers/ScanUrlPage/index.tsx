import withLayout from "../../HOCs/withLayout";
import ScanUrlView from "../../components/ScanUrlView";

const ScanUrlPage = () => {
  return <ScanUrlView />;
};

export default withLayout(ScanUrlPage);
